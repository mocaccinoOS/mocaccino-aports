package_dir: /luetbuild/pkg/{{.Values.name}}
steps:
  - sh build.sh
requires:
  - category: alpine
    name: postgresql-dev
    version: '>=0'
  - category: alpine
    name: tcl-dev
    version: '>=0'
  - category: alpine
    name: docbook2x
    version: '>=0'
  - category: development
    name: toolchain
    version: '>=0'
