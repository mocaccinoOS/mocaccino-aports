name: fuse
version: "2.9.9"
category: alpine
description: A library that makes it possible to implement a filesystem in a userspace program.
uri:
  - https://github.com/libfuse/libfuse/
license: GPL-2.0-only LGPL-2.1-only
labels:
  package.source: "https://github.com/libfuse/libfuse/releases/download/fuse-2.9.9/fuse-2.9.9.tar.gz"
requires:
  - category: alpine
    name: "fuse-common"
    version: '>=0'
