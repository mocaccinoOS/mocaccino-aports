name: fuse3
version: "3.10.0"
category: alpine
description: The reference implementation of the Linux FUSE (Filesystem in Userspace) interface
uri:
  - https://github.com/libfuse/libfuse
license: GPL-2.0-only LGPL-2.1-only
labels:
  package.source: "https://github.com/libfuse/libfuse/releases/download/fuse-3.10.0/fuse-3.10.0.tar.xz"
requires:
  - category: alpine
    name: "fuse-common"
    version: '>=0'
