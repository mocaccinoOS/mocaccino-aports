name: sshfs
version: "3.7.1"
category: alpine
description: FUSE client based on the SSH File Transfer Protocol
uri:
  - https://github.com/libfuse/sshfs
license: GPL-2.0-or-later
labels:
  package.source: "https://github.com/libfuse/sshfs/releases/download/sshfs-3.7.1/sshfs-3.7.1.tar.xz"
requires:
  - category: alpine
    name: "openssh-client"
    version: '>=0'
  - category: alpine
    name: "fuse3"
    version: '>=0'
