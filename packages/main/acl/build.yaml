package_dir: /luetbuild/pkg/{{.Values.name}}
steps:
  - sh build.sh
requires:
  - category: alpine
    name: attr-dev
    version: '>=0'
  - category: alpine
    name: libattr
    version: '>=0'
  - category: development
    name: toolchain
    version: '>=0'
